MySQL info:
{{.Values.mysql.service.type}}
export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app={{ include "resterapp-chart.name" . }},release={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
